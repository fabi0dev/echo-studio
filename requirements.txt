# Echo Studio — local SD 1.5 image generation (DreamShaper 8)
# torch-directml pins torch 2.4.1. transformers 5.x requires torch>=2.5, so stay on 4.x.
# diffusers 0.40 wants huggingface_hub>=1.23; transformers 4.x wants hub<1.
# After `pip install -r requirements.txt`, run:
#   pip install "transformers>=4.44,<5" --no-deps

fastapi>=0.115
uvicorn[standard]>=0.30
python-dotenv>=1.0
pillow>=10.3

huggingface_hub>=0.24
tokenizers>=0.22,<0.23
safetensors>=0.4
omegaconf>=2.3
accelerate>=0.34
diffusers>=0.40.0,<0.41
